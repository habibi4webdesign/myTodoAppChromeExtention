import { makeStyles } from 'theme'

const useTimePickerStyle = makeStyles(
  ({ spacing, palette: { grey } }) => ({
    root: {},
  }),
  {
    name: 'TimePicker',
  },
)

export default useTimePickerStyle
